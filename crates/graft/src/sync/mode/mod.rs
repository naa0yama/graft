/// CI drift-detection mode (`--ci-check`)
pub mod ci_check;
/// Patch-file regeneration mode (`--patch-refresh`)
pub mod patch_refresh;
/// Default sync mode and dry-run (`--dry-run`)
pub mod sync;
/// Manifest validation mode (`--validate`)
pub mod validate;
